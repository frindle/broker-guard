# --- Python ---
__pycache__/
*.pyc
.venv/

# --- Penn's PII / identity (NEVER commit) ---
.env
profile.local.json
profile.*.local.json
*.local.json

# --- runtime data tied to Penn's identity (NEVER commit) ---
data/state.sqlite
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
logs/
results/
serpwatch-hits/
removals/
*.removal.json

# --- UI-editable runtime settings (see broker_guard/settings.py). Written by
# the /settings page onto the data volume; holds a third-party API key, and is
# per-deployment state, not source. ---
data/settings.json
settings.json

# --- broker dataset: maintained in the SEPARATE PUBLIC repo, pulled at runtime ---
data/brokers.json

# --- vendored removal engine (cloned, not committed) ---
vendor/eraser/

# --- docker runtime volumes (PII: profile, state db, logs, eraser config) ---
state/
alerts.jsonl
.env.local

# --- dispatch harness leakage ---
.preflight-state.json

# --- self-monitoring / health (PII-bearing: snapshots, dispatch queue, reports) ---
snapshots/
needs-fix/
heartbeat.json
health-report.txt
run-log.jsonl

# --- automated escalation (PII-bearing: letters, complaints, audit trail) ---
escalations/
letters/
complaints/
audit-log.jsonl
escalation-rules.json
